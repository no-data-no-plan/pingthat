{
  "name": "pingthat",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/early-hints.mjs && node scripts/build-es-paths.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild": "bash scripts/update-sitemap-lastmod.sh"
  },
  "dependencies": {
    "@astrojs/svelte": "^8.0.3",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.0.8",
    "svelte": "^5.55.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
