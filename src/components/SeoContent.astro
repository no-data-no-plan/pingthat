---
import { tools } from "../lib/tools";

interface Props {
  heading: string;
  text: string;
  relatedToolIds: string[];
}

const { heading, text, relatedToolIds } = Astro.props;
const relatedTools = relatedToolIds
  .map((id) => tools.find((t) => t.id === id))
  .filter(Boolean) as typeof tools;
---

<section class="px-8 py-5 border-t border-[var(--color-border)]" style="background: var(--color-surface);">
  <h2 class="text-[10px] uppercase tracking-[0.12em] mb-2" style="color: var(--color-accent);">{heading}</h2>
  <p class="text-xs leading-relaxed" style="color: var(--color-text-muted);">{text}</p>
  {relatedTools.length > 0 && (
    <div class="flex flex-wrap gap-2 mt-3">
      <span class="text-[10px]" style="color: var(--color-text-dim);">Related:</span>
      {relatedTools.map((tool) => (
        <a href={tool.path} class="text-[10px] hover:underline" style="color: var(--color-accent);">
          {tool.name}
        </a>
      ))}
    </div>
  )}
</section>
