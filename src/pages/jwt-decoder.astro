---
import Layout from "../layouts/Layout.astro";
import JwtDecoder from "../components/JwtDecoder.svelte";
import SeoContent from "../components/SeoContent.astro";
import SeoBlock from "../components/SeoBlock.astro";
import ToolContent from "../components/ToolContent.astro";
import { pageI18n } from "../i18n/pages";

const p = pageI18n["jwt-decoder"].en;
---

<Layout
  title={p.title}
  description={p.description}
  activeToolId="jwt-decoder"
>
  <JwtDecoder client:load />

  <SeoContent
    heading={p.seoHeading}
    text={p.seoText}
    relatedToolIds={["password-strength", "privacy-check", "ip-converter"]}
  />

  <ToolContent
    howHeading={p.howHeading}
    howSteps={p.howSteps}
    useCasesHeading={p.useCasesHeading}
    useCases={p.useCases}
    faqHeading={p.faqHeading}
    faqs={p.faqs}
  />

    <SeoBlock
    heading={p.seoBlockHeading}
    text={p.seoBlockText}
    features={p.seoFeatures}
  />
</Layout>
